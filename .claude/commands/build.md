Build le site pour la production.

Lance le build Astro, vérifie qu'il n'y a pas d'erreurs, et affiche un résumé de la taille du build.

```
pnpm build
```

Si le build échoue, analyse les erreurs et propose des corrections.
