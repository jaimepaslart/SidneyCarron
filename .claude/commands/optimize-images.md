Analyse et optimise les images du projet.

1. Liste toutes les images dans `src/assets/` et `public/`
2. Vérifie les formats (préférer WebP/AVIF pour le web)
3. Vérifie les dimensions (pas d'images surdimensionnées)
4. Vérifie que les composants Astro utilisent bien `<Image>` ou `<Picture>` d'`astro:assets` pour le lazy loading et le responsive
5. Propose des améliorations concrètes

Rappel : pour un site d'artiste, la qualité visuelle est primordiale. On optimise la taille sans sacrifier la qualité perçue.
