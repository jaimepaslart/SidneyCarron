Recherche dans la documentation GSAP (GreenSock Animation Platform) via Context7.

Utilise le MCP Context7 avec le library ID `/llmstxt/gsap_llms_txt` pour répondre à la question de l'utilisateur.

Si la question est vague, demande des précisions. Sinon, utilise `query-docs` avec :
- libraryId: `/llmstxt/gsap_llms_txt`
- query: la question posée

Résume la réponse de manière concise avec des exemples de code si pertinent.

$ARGUMENTS
