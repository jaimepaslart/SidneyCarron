Recherche dans la documentation Motion (anciennement Framer Motion) via Context7.

Utilise le MCP Context7 avec le library ID `/websites/motion_dev` pour répondre à la question de l'utilisateur.

Si la question est vague, demande des précisions. Sinon, utilise `query-docs` avec :
- libraryId: `/websites/motion_dev`
- query: la question posée

Résume la réponse de manière concise avec des exemples de code si pertinent.

$ARGUMENTS
