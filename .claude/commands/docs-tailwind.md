Recherche dans la documentation Tailwind CSS via Context7.

Utilise le MCP Context7 avec le library ID `/websites/v3_tailwindcss` pour répondre à la question de l'utilisateur.

Si la question est vague, demande des précisions. Sinon, utilise `query-docs` avec :
- libraryId: `/websites/v3_tailwindcss`
- query: la question posée

Résume la réponse de manière concise avec des exemples de code si pertinent.

$ARGUMENTS
